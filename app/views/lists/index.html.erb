<div class="container py-5">
 <div class="text-center">
        <h1><strong> Add a movie in your list </strong></h1>

          <%= link_to "Create a movie list", new_list_path, class: "btn btn-secondary" %>

<div style="background-image: url(<%= asset_path('download-1.jpg') %>); background-repeat: no-repeat; background-size: cover">


 <%# <div class="text-center">
        <h1><strong> Add a movie in your list </strong></h1> %>


        <div class="container my-5">
        <div class="row">
        <% @lists.each do |list| %>
            <div class="co-12 col-sm-6 col-md-4 mb-3">
                <%= link_to list_path(list), class:"text-decoration-none" do %>
                <div class="container py-5" >
                    <div class="card-category" style="border-width:2px; border-style: inset; border-radius: 40%; border-color: white" >
                       <p><%= list.name %></p>
                    </div>
                    <% end %>
                    </div>
            </div>
            <% end %>
       </div>
       </div>
    </div>
 </div>